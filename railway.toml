[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
# Use Dockerfile CMD (start.sh script handles PORT variable)
# startCommand is not needed - Dockerfile CMD will be used
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
